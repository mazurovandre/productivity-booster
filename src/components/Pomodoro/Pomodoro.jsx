import React from 'react';
import SoundWidget from "../SoundCloud/SoundWidget";

const Pomodoro = () => {
    return (
        <div>

            <SoundWidget/>
        </div>
    );
};

export default Pomodoro;